# Legacy code

Older prototypes kept for reference. Prefer **`main.py`** at the repo root for new runs.

| Path | Role |
|------|------|
| `grover_adaptive_controller.py` | Early feature-based strategy picker (closed-loop with Fake backends) |
| `fake_backends/` | Custom Fake Kyoto / Sherbrooke helpers |
| `rcoh.py` | Early `R_coh` / coherence explorer with Aer noise |
